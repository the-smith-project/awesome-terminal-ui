# GlyphUI – Agent Brief

## Repository
- Path: `~/repos/glyphui`
- Project channel: `#projekt-a` (Discord server HERMESAGENTSWARM)

## Mission
Collect, organize, and build the GlyphUI knowledge base around Terminaiui.dir (terminal/TUI interaction patterns). Keep research artifacts under version control-ready structure for future implementation work.

## Workflow Guidelines
1. Always read both `AGENTS.md` and `MEMORY.md` before executing new tasks.
2. Store incoming research material inside `research/`, using slugified filenames derived from the provided headline.
3. Preserve verbatim content unless explicitly asked to summarize. Add context notes to MEMORY when helpful.
4. Whenever implementation begins, scaffold cleanly inside `~/repos/glyphui` and document major decisions in `MEMORY.md`.
5. Assume this repo may later be initialized as a git project; keep files deterministic and organized.
